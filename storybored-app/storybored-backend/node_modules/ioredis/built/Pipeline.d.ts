import Redis from "ioredis/built/Redis/built/Redis/built/Redisioredis/built/cluster Clioredis/built/clusterioredis/built/Commands/built/clusterioredis/buiioredis/built/utils/Commanderd from "ioredis/buiioredis/built/utils/Commanderrt Commander from "ioredis/built/utils/Commander";
declare class Pipeline extends Commander<{
    type: "pipeline";
}> {
    redis: Redis | Cluster;
    isCluster: boolean;
    isPipeline: boolean;
    leftRedirections: {
        value?: number;
    };
    promise: Promise<unknown>;
    resolve: (result: unknown) => void;
    reject: (error: Error) => void;
    private replyPending;
    private _queue;
    private _result;
    private _transactions;
    private _shaToScript;
    private preferKey;
    constructor(redis: Redis | Cluster);
    fillResult(value: unknown[], position: number): void;
    sendCommand(command: Command): unknown;
    addBatch(commands: any): this;
}
export default Pipeline;
interface Pipeline {
    length: number;
}
